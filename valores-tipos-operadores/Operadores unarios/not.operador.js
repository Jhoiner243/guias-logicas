// Invierte los bits de un número. Menos común, pero útil para ciertas operaciones.



// Verificar si un elemento existe en un array (alternativa a indexOf)
const index = array.indexOf(item);
if (~index) { // Equivale a index !== -1
  console.log("Elemento encontrado");
}