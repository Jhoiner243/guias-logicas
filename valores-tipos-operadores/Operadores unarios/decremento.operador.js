// Similar al incremento pero en este caso decrementa el valor de la variable en 1.

// Temporizador de cuenta regresiva
let timer = 5;

while (timer > 0) {
  console.log(timer--); // Muestra 5, 4, 3, 2, 1
}